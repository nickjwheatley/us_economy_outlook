[project]
name = "market-outlook"
version = "0.1.0"
description = "Deterministic US economic outlook scoring model and dashboard prototype."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
